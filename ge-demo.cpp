#include "game_engine.h"

void Test(){
	/*
	A2W
	W2A
	*/

	/*
	_msize
	malloc
	calloc
	_alloca
	_expand
	realloc
	_recalloc

	_aligned_msize
	_aligned_malloc
	_aligned_realloc
	_aligned_recalloc
	*/

	/*
	rint  // 四舍六入五取偶
	modf  // x - [x], y = [x]
	ceil  // ⌈x⌉
	floor // ⌊x⌋
	trunc // [x]
	round // ⌊x + 0.5⌋

	div       // x / y
	fmod      //求模(余数绝对值最小)
	remainder //求余(商最小)
	remquo    //求余,商赋给z

	erf  //高斯误差
	erfc //补余高斯误差

	tgamma // gamma(x)
	lgamma // ln(|gamma(x)|)

	fma   // x*y + z
	fdim  // max(x - y, 0)
	hypot // √(x² + y²)
	log1p // logₑ(x + 1)
	expm1 // eˣ − 1
	ldexp // x * 2ʸ
	frexp // x = return * 2ʸ

	copysign // sign(y)|x|
	*/

	/*
	dx::XMConvertVectorIntToFloat
	dx::XMConvertVectorFloatToInt
	dx::XMConvertVectorUIntToFloat
	dx::XMConvertVectorFloatToUInt
	*/

	/*
	dx::Internal::PointOnLineSegmentNearestPoint();
	dx::Internal::PointOnPlaneInsideTriangle();
	dx::Internal::XMPlaneTransform();
	dx::Internal::XMVector3AllTrue();
	dx::Internal::XMVector3AnyTrue();
	dx::Internal::XMColorHue2Clr();
	dx::Internal::SolveCubic();
	*/
}